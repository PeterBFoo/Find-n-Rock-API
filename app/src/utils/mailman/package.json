{
  "name": "mailman",
  "version": "1.0.0",
  "description": "NPM module to send emails using Nodemailer",
  "main": "mailman.js",
  "scripts": {
    "test": "npx tsc && jest --forceExit --detectOpenHandles --runInBand"
  },
  "keywords": [
    "mail"
  ],
  "author": "PeterBFoo",
  "license": "ISC",
  "dependencies": {
    "@sendgrid/mail": "^7.7.0",
    "dotenv": "^16.0.3"
  },
  "devDependencies": {
    "jest": "^29.5.0"
  }
}
